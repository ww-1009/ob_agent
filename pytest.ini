[pytest]
# 与 README「tests/ Backend pytest (Repository root, pytest.ini testpaths=tests)」对齐。
# pythonpath 让后端包 app 与测试辅助包 helpers 在仓库根直接可导入，
# 无需再手动设置 PYTHONPATH=backend:tests。
testpaths = tests
pythonpath = backend tests